# Nexus

注意力保护系统。按 `DESIGN.md` 和 `docs/modules/*.md` 实现首版 Web SPA。

## 当前实现

- 六大主页面：仪表盘、事件流、任务、执行器、规则引擎、信息源
- 工作空间切换
- 右侧详情面板
- 右侧对话侧板
- 本地 mock 数据驱动

## 启动

```bash
pnpm install
pnpm dev
```

## 验证

```bash
pnpm test
pnpm build
```
