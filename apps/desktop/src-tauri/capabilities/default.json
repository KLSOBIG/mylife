{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for main and widget windows.",
  "windows": ["main", "widget"],
  "permissions": [
    "core:default"
  ]
}
